# Implementation Plans - {{PROJECT_NAME}}

This directory holds downstream implementation plans for `{{TARGET_REPO}}`.

## Source Inputs

- Canonical spec store: `{{SOURCE_SPEC_STORE_URL}}`
- Source spec: `{{SOURCE_SPEC_PATH}}`
- Local source-spec copy: `docs/source-specs/{{SOURCE_SPEC_FILENAME}}`
- App ID: `{{APP_ID}}`
- Source app for research orientation: `{{APP_NAME}}`

## Planning Rules

- Treat the source spec as the implementation contract unless later evidence corrects it.
- Keep this project non-affiliated and avoid proprietary names as project branding.
- Use original code, original assets, synthetic seed data, and public documentation only.
- Mark account-gated, paid, regional, native-device, regulated, hardware, and third-party approval behavior as blocked until verified.
- Record corrections that belong in the canonical spec store before relying on them downstream.

## Initial Plan Scaffold

### Product Surface

- Primary workflows:
- Secondary workflows:
- Out-of-scope V1 behavior:
- Manual verification blockers:

### Technical Direction

- Application stack:
- Data model:
- API surface:
- Authentication and authorization:
- Offline and sync behavior:
- Test strategy:

### Risks

- Legal/name/attribution:
- Privacy and safety:
- Third-party services:
- Device or platform dependencies:
- Data provenance:

### Evidence Log

- Seeded from `{{SOURCE_SPEC_PATH}}`:
- Manual verification evidence:
- Source-spec corrections sent upstream:
